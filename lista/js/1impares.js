//1. Imprimir os números impares menores que 100 e exibir na janela do navegador, linha a linha com o título "Número 1", "Número 3" ...

for(i=1;i<100;i++)
    if (i % 2 == 0){ //compara se o numero é divisivel por 2
    }
    else { // se não for, então digite o numero, assim pegando só os impares
        alert("Numero "+i)
    }
    